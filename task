1)method using to find rating of movie
class Movie{
    constructor(a,b,c){
        this.title=a
        this.studio=b
        this.rating=c
    }
    getPG(){
        var temp=[]
        temp.push(`${this.title} movie rating is ${this.rating}`)   
        return temp 
    }
}
   
var mov=new Movie("Leo","kalai","PG13")
console.log(mov.getPG())
  2)method using find circumstance and radius
  class circle{
    constructor(radius,color){
        this.r=radius
        this.c=color
    }
    getRadius(){
        return this.r
    }
    getArea(){
        this.area=(Math.PI)*(Math.pow(this.r,2))
        return this.area
    }
    getColor(){
        return this.c
    }
    getCircum(){
      this.circum=2*(Math.PI)*this.r
      return this.circum
    }
}
var maths=new circle(2,"red")
console.log(maths.getRadius())
console.log(maths.getArea())
console.log(maths.getColor())
console.log(maths.getCircum())
  
